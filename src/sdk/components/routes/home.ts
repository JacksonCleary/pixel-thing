import { Home } from '../../classes/routes/Home';
import { IDirector } from '../../classes/Director';

const initialize = (director, title) => {
  window.___debug.log('Initializing Home route', 'success');
};

export default initialize;
